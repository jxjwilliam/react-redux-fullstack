1. use mongodb

```bash

curl -X POST "http://localhost:8088/api/tasks"  -H "Content-Type: application/x-www-form-urlencoded"  -d 'description=test'

mongoimport  -d redux -c users  ./etc/data.json --jsonArray

mongoimport --db redux --collection users < ./etc/data.json  --jsonArray


DATA='{"counter":11}'
curl -X POST "http://localhost:8081/api/counter" -H "Content-Type: application/json" -d "${DATA}"
```

