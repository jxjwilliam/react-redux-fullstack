import todos from './todos'
import users from './users'
import counter from './counter'
import auth from './auth'

module.exports = {
  todos: todos,
  users: users,
  counter: counter,
  auth: auth
}
